{
  "name": "travelers.ai",
  "version": "0.1.0",
  "private": true,
  "description": "AI-powered travel planning API with educational POI content",
  "scripts": {
    "dev:api": "cd packages/api && uvicorn travelers_api.main:app --reload --port 8000"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
